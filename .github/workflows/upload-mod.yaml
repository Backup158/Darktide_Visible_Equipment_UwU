name: Upload Mod to NexusMods

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number for the release'
        required: true

jobs:
  upload-to-nexusmods:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Zip mod files
        run: zip -r visible_equipment_uwu.zip visible_equipment_uwu/*

      - name: Upload mod to NexusMods
        id: upload
        uses: Nexus-Mods/upload-action@main
        with:
          api_key: ${{ secrets.NEXUSMODS_API_KEY }}
          file_id: ${{ vars.NEXUSMODS_FILE_ID }}
          filename: visible_equipment_uwu.zip
          display_name: "Visible Equipment - Uprooted with Umbrage (UwU)"
          description: "https://backup158.github.io/Darktide_Visible_Equipment_UwU/CHANGELOG"
          changelog: "https://backup158.github.io/Darktide_Visible_Equipment_UwU/CHANGELOG"
          version: ${{ github.event.release.tag_name || github.event.inputs.version }}
          update_mod_version: true
